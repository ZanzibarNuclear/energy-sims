# Fixtures

Example plant and load configuration JSON for headless runs and tests.

Stage 1 will add hydro plant fixtures and station load registries once config schemas land (PR2 / PR4). Keep committed fixtures small and teaching-oriented; ignore ad-hoc run outputs via the root `.gitignore`.
